# ORDER BY
Com o comando `SELECT actor_id, first_name FROM actor`, nos selecionamos as colunas `actor_id` e `first_nane` da tabela `actor`

Com o `ORDER BY` é possivel ordernar como vai mostrar as colunas. No caso podemos ordernar por meio da coluna `first_name`

A query ficaria da seguinte forma
`SELECT actor_id, first_name, last_name FROM actor ORDER BY first_name;`